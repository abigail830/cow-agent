# Frontend env (Vite). Copy to .env.local for local overrides.
#
# --- 方式 A（推荐）：Vercel rewrite 代理 /api，不要设置此项 ---
# 若在 Vercel 设置了 VITE_API_BASE_URL 会跨域并触发 CORS，请删除该变量后 Redeploy。
# VITE_API_BASE_URL=
#
# --- 方式 B：跨域直连（需后端 CORS_ORIGINS + AUTH_COOKIE_SAMESITE=none）---
# VITE_API_BASE_URL=https://agent-team-backend.vercel.app
